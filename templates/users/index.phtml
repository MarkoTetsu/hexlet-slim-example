<p>
    <a href="/users/new">Новый пользователь</a>
</p>
<p>
    <form action="/users" method="get">
        <input type="search" required name="term">
    </form>
</p>
<p>
    <div>
        <?php foreach ($users as ['nickname' => $nickname, 'id' => $id]): ?>
            <div>
                    <?php echo "<a href='/user/{$id}'>{$nickname}</a>" ?>
            </div>
        <?php endforeach ?>
    </div>
</p>
